(ns socket-go.cljs.index
  (:use-macros [c2.util :only [p pp bind!]])
  (:use [c2.core :only [unify]])
  (:require [c2.core :as c2]
            [c2.scale :as scale]
            [c2.dom :as dom]
            [c2.event :as event]))

(defn ^:export init []
  )






